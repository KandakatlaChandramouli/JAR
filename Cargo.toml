[package]
name = "jar"
version = "0.2.0"
edition = "2021"
description = "A progressively extensible Linux process-isolation runtime prototype"
authors = ["JAR Project"]

[dependencies]
nix = { version = "0.29", features = ["process", "sched", "mount", "user", "sched"] }
