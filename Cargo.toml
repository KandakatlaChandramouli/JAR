[package]
name = "jar"
version = "0.9.0"
edition = "2021"
description = "A progressively extensible Linux process-isolation runtime prototype"
authors = ["JAR Project"]

[dependencies]
nix = { version = "0.29", features = ["process", "sched", "mount", "user", "fs"] }
libseccomp = "0.3"
libc = "0.2"
caps = "0.5"
tar = "0.4"
flate2 = "1.0"
